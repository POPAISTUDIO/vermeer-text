diff --git a/node_modules/@librechat/agents/dist/cjs/summarization/node.cjs b/node_modules/@librechat/agents/dist/cjs/summarization/node.cjs
index 727f37a..cae1458 100644
--- a/node_modules/@librechat/agents/dist/cjs/summarization/node.cjs
+++ b/node_modules/@librechat/agents/dist/cjs/summarization/node.cjs
@@ -390,6 +390,8 @@ function describeFallbackError(err, fallbacks) {
 async function executeSummarizationWithFallback(params) {
     const { agentContext, messages: messages$1, clientConfig, summarizeConfig, stepId, usePromptCache, log, } = params;
     const priorSummaryText = agentContext.getSummaryText()?.trim() ?? '';
+    const isSelfSummarizeModel = clientConfig.provider === agentContext.provider;
+    const toolsForBinding = isSelfSummarizeModel ? agentContext.getToolsForBinding() : [];
     let summaryText = '';
     let summaryUsage;
     try {
@@ -401,7 +403,7 @@ async function executeSummarizationWithFallback(params) {
         const summarizationModel = init.initializeModel({
             provider: clientConfig.provider,
             clientOptions: clientConfig.clientOptions,
-            tools: agentContext.getToolsForBinding(),
+            tools: toolsForBinding,
         });
         const result = await summarizeWithCacheHit({
             model: summarizationModel,
@@ -437,7 +439,7 @@ async function executeSummarizationWithFallback(params) {
                 });
                 const fbResult = await invoke.tryFallbackProviders({
                     fallbacks,
-                    tools: agentContext.getToolsForBinding(),
+                    tools: [],
                     messages: [
                         ...messages$1,
                         new messages.HumanMessage(buildSummarizationInstruction(clientConfig.promptText, clientConfig.updatePromptText, priorSummaryText)),
diff --git a/node_modules/@librechat/agents/dist/esm/summarization/node.mjs b/node_modules/@librechat/agents/dist/esm/summarization/node.mjs
index 4730b13..640a0aa 100644
--- a/node_modules/@librechat/agents/dist/esm/summarization/node.mjs
+++ b/node_modules/@librechat/agents/dist/esm/summarization/node.mjs
@@ -388,6 +388,8 @@ function describeFallbackError(err, fallbacks) {
 async function executeSummarizationWithFallback(params) {
     const { agentContext, messages, clientConfig, summarizeConfig, stepId, usePromptCache, log, } = params;
     const priorSummaryText = agentContext.getSummaryText()?.trim() ?? '';
+    const isSelfSummarizeModel = clientConfig.provider === agentContext.provider;
+    const toolsForBinding = isSelfSummarizeModel ? agentContext.getToolsForBinding() : [];
     let summaryText = '';
     let summaryUsage;
     try {
@@ -399,7 +401,7 @@ async function executeSummarizationWithFallback(params) {
         const summarizationModel = initializeModel({
             provider: clientConfig.provider,
             clientOptions: clientConfig.clientOptions,
-            tools: agentContext.getToolsForBinding(),
+            tools: toolsForBinding,
         });
         const result = await summarizeWithCacheHit({
             model: summarizationModel,
@@ -435,7 +437,7 @@ async function executeSummarizationWithFallback(params) {
                 });
                 const fbResult = await tryFallbackProviders({
                     fallbacks,
-                    tools: agentContext.getToolsForBinding(),
+                    tools: [],
                     messages: [
                         ...messages,
                         new HumanMessage(buildSummarizationInstruction(clientConfig.promptText, clientConfig.updatePromptText, priorSummaryText)),
